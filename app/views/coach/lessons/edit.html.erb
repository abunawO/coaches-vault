<% back_path = safe_return_path(lessons_path) %>

<div class="lesson-form-page">
  <%= render "shared/page_header",
        breadcrumbs: [["Dashboard", root_path], ["Lessons", back_path], ["Edit Lesson", edit_coach_lesson_path(@lesson)]],
        title: content_tag(:span, "✏️", class: "page-title-icon") + " Edit Lesson",
        subtitle: "Update your lesson details.",
        primary_action: { label: "Back to lessons", url: back_path } %>

  <%= render "form" %>
</div>
